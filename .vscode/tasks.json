{
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build all",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/LightningCore"
            },
            "command": "make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}